import type { Config } from 'prettier';

const config: Config = {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  tabWidth: 2,
  printWidth: 100,
  useTabs: false,
  arrowParens: 'always',
  endOfLine: 'lf',
};

export default config;
